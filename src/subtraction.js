// Mark this static page so the shared home script opens the subtraction workspace.
document.addEventListener("DOMContentLoaded", () => {
	document.body.dataset.page = "subtraction";
	document.body.dataset.subject = "subtraction";
});
